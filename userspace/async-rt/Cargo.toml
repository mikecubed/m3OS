[package]
name = "async-rt"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = ["libc"]
alloc = []

[dependencies]
syscall-lib = { path = "../syscall-lib" }
libc = { version = "0.2", optional = true }

[dev-dependencies]
libc = "0.2"
