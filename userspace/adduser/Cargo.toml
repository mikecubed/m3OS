[package]
name = "adduser"
version.workspace = true
edition.workspace = true

[[bin]]
name = "adduser"
path = "src/main.rs"

[dependencies]
# `alloc` for the argon2id password hash (Phase 110 Track C).
syscall-lib = { path = "../syscall-lib", features = ["argon2"] }
shadow = { path = "../lib/shadow" }
