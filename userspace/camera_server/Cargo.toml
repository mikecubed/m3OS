[package]
name = "camera_server"
version = "0.92.0"
edition = "2024"
description = "Phase 92c — ring-3 camera IPC server (UVC frame aggregator)."

[[bin]]
name = "camera_server"
path = "src/main.rs"

[dependencies]
driver_runtime = { path = "../lib/driver_runtime" }
syscall-lib = { path = "../syscall-lib", features = ["alloc"] }
kernel-core = { path = "../../kernel-core", default-features = false }
