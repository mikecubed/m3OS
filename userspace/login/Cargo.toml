[package]
name = "login"
version.workspace = true
edition.workspace = true

[[bin]]
name = "login"
path = "src/main.rs"

[dependencies]
# `alloc` for the argon2id verify + re-hash path (Phase 110 Track C).
syscall-lib = { path = "../syscall-lib", features = ["argon2"] }
passwd = { path = "../passwd", default-features = false }
