[package]
name = "lockscreen"
version.workspace = true
edition.workspace = true
description = "Phase 73 — lockscreen Layer-shell client with /etc/shadow password authentication"

[[bin]]
name = "lockscreen"
path = "src/main.rs"

[dependencies]
syscall-lib = { path = "../syscall-lib", features = ["alloc"] }
kernel-core = { path = "../../kernel-core", default-features = false }
desktop_client = { path = "../lib/desktop_client" }
