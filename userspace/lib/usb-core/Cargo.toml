[package]
name = "usb-core"
version = "0.78.2"
edition = "2024"
description = "Phase 78b Track A — host↔class IPC protocol types for USB class drivers."

[dependencies]
kernel-core = { path = "../../../kernel-core", default-features = false }
