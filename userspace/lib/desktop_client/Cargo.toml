[package]
name = "desktop_client"
version = "0.73.0"
edition = "2024"
description = "Phase 73 — shared compositor-client boilerplate (handshake, SHM, text rendering)"

[lib]
name = "desktop_client"
path = "src/lib.rs"

[dependencies]
syscall-lib = { path = "../../syscall-lib", features = ["alloc"] }
kernel-core = { path = "../../../kernel-core", default-features = false }
