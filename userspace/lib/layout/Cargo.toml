[package]
name = "layout"
version.workspace = true
edition.workspace = true
description = "Phase 72 — pure-logic tiling layout policies for the m3OS display_server compositor."

[lib]
name = "layout"
path = "src/lib.rs"

[dependencies]
kernel-core = { path = "../../../kernel-core", default-features = false }
