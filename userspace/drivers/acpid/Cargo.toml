[package]
name = "acpid"
version.workspace = true
edition.workspace = true
description = "Phase 101 Track E — ring-3 ACPI daemon: AML namespace host, device/resource query service, SCI event dispatch."

[[bin]]
name = "acpid"
path = "src/main.rs"

[dependencies]
syscall-lib = { path = "../../syscall-lib", features = ["alloc"] }
kernel-core = { path = "../../../kernel-core", default-features = false }
