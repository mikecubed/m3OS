[package]
name = "i2c_hid"
version.workspace = true
edition.workspace = true
description = "Phase 102 — ring-3 I2C-HID touchpad driver (Intel LPSS DesignWare I2C)."

[[bin]]
name = "i2c_hid"
path = "src/main.rs"

[dependencies]
syscall-lib = { path = "../../syscall-lib", features = ["alloc"] }
kernel-core = { path = "../../../kernel-core", default-features = false }
