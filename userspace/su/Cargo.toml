[package]
name = "su"
version.workspace = true
edition.workspace = true

[[bin]]
name = "su"
path = "src/main.rs"

[dependencies]
# `alloc` so `verify_password`'s argon2id arm is compiled in (Phase 110).
syscall-lib = { path = "../syscall-lib", features = ["argon2"] }
