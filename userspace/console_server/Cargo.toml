[package]
name = "console_server"
version = "0.53.0"
edition = "2024"

[[bin]]
name = "console_server"
path = "src/main.rs"

[dependencies]
syscall-lib = { path = "../syscall-lib", features = ["alloc"] }
kernel-core = { path = "../../kernel-core", default-features = false }
