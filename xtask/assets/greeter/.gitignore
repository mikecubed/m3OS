# Phase 71 follow-up — `background.png` is committed (a small, shared
# boot artifact). Drop `background.bmp` here if you want to override
# at build time; only the BMP variant is gitignored to keep the
# canonical PNG image stable across clones and CI.
background.bmp
