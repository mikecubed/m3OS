ostest Phase 8 — read-only ramdisk demo.

Files in this image:
  hello.txt  - greeting from the filesystem layer
  readme.txt - this file

The ramdisk is embedded in the kernel binary at build time.
Writable filesystems are deferred to Phase 9+.
